//this class is like something to do with memory
var TSOS;
(function (TSOS) {
    var memory = (function () {
        function memory() {
        }
        return memory;
    }());
    TSOS.memory = memory;
})(TSOS || (TSOS = {}));
